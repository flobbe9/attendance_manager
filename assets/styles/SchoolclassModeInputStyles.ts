import { DynamicStyle } from "@/abstract/DynamicStyle";
import { ViewStyle } from "react-native";

export class SchoolclassModeInputStyles {
    static component: DynamicStyle<ViewStyle> = {
        default: {},
    };
    
    static radioButtonColor = "rgb(180, 180, 180)";
}
