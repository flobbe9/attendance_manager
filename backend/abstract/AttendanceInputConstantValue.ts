/**
 * The value of an object containing attandance input constants.
 * 
 * @since latest
 */
export interface AttendanceInputConstantValue {
    /** Ordinal value */
    index: number,
    /** Indicates whether this value should be considered during input validation */
    validate: boolean
}